## Summary

<!-- What changed and why (one short paragraph is enough). -->

## Checklist

- [ ] `make check` passes locally (or equivalent: pre-commit + tests)
- [ ] Docs updated if behavior or developer workflow changed
